{
  // --- eslint
  "[javascript]": {
    // Disable format on save if enabled for tools like (default formatter, prettier ...).
    "editor.formatOnSave": false,
  },
  // Disable default editor validation to enable eslint validation.
  "javascript.validate.enable": false,
}
